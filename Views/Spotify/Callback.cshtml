<main class="no-scroll">
    <div class="v-align">
        <div class="callback-redirect font-white">
            <span class="font-heading font-big">@ViewData["state"]</span><br>
            <span class="font-content font-medium">##redirecting to##</span>
        </div>
    </div>
</main>